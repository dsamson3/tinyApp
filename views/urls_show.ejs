<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>TinyApp</title>
</head>
<body>
    <header><% include partials/_header %></header>
    <br>
    <h1>Your shortened URL</h1>
    <h3><%= urls[shortURL]['longURL'] %> has been shortened to <a href=http://localhost:8080/u/<%= shortURL %>>http://localhost:8080/u/<%= shortURL %></a></h3>
    <form action="/urls/<%= shortURL %>/edit" method="POST">
        <input id="longURL" type="text" name="longURL" placeholder="<%= urls[shortURL]['adr'] %>" style="width: 300px">
        <input type="submit" value="Update">
      </form>
      <form method="POST" action="/urls/<%= shortURL %>/delete"><input type="submit" class="btn" value="Delete Me" /></form>
    <hr />
</body>
</html>